version: '3.5'

services:
  ubuntu:
    build:
      context: ./
    image: vivasa/ubuntu:learn
    container_name: ubuntum
    hostname: ubuntum
    environment:
      TERM: xterm-256color
    ports:
      - "80"
    tty: true
